# =============================================================================
# PTQ × LayerSkip Benchmark — Requirements
# =============================================================================
# Extends LayerSkip's requirements.txt with PTQ + energy measurement libs.
#
# Install:
#   pip install -r scripts/requirements.txt
#
# NOTE: Install PyTorch with CUDA first via conda (before pip):
#   conda install pytorch==2.2.1 torchvision==0.17.1 torchaudio==2.2.1 \
#         pytorch-cuda=12.1 -c pytorch -c nvidia
# =============================================================================

# --- LayerSkip base (pinned to match LayerSkip/requirements.txt exactly) ---
accelerate==1.0.1
numpy==1.26.4
transformers==4.50.0
torchmetrics==1.3.2
torcheval==0.0.7
sentencepiece==0.2.0
datasets==3.0.1
pandas==2.2.2
tabulate==0.9.0
colorama==0.4.6
protobuf==5.28.2
lm_eval==0.4.5
pytest==8.3.3
pytest-mock==3.14.0
matplotlib==3.10.0

# --- PTQ: bitsandbytes (naive int8 / W8A8) ---
bitsandbytes>=0.43.0

# --- PTQ: AWQ W4A16 ---
autoawq>=0.2.0

# --- PTQ: GPTQ W4A16 ---
# Install separately with CUDA wheel for best compat:
#   pip install auto-gptq --extra-index-url \
#       https://huggingface.github.io/autogptq-index/whl/cu121/
auto-gptq>=0.7.1

# --- PTQ: SmoothQuant W8A8 ---
# If pip install fails, install from source:
#   pip install git+https://github.com/mit-han-lab/smoothquant.git
smoothquant>=0.1.1

# --- Energy measurement ---
pynvml>=11.5.0

# --- Plotting ---
seaborn>=0.13.0
